# DECISIONS

- 2026-06-12 — MISSION 2 CHARTER: high-conversion sales landing page. RESEARCH (BD f-commerce conversion/pricing/pixels) + DESIGN (LANDING_DESIGN.md from dark AI reference) dispatched in parallel; both delivered. Research confirms market white-space: no Bangla-first, COD/courier-aware, bKash-native WhatsApp CRM competitor; 3-tier decoy validated; pixel taxonomy locked.
- 2026-06-12 — FOUNDER added FLAGSHIP value prop: **customer-data ownership + ban-proofing**. On Meta-owned channels the seller doesn't own their data; a page/number ban = lost customers + lost data. We store their contacts/chats/orders securely (survives a ban), make it exportable/reusable, and offer follow-up + funnel automation to re-engage — plus anti-ban WhatsApp safety. This becomes a hero-adjacent flagship section + a real data-export feature; funnel automation = fast-follow (Marketing module). HONEST scope flagged at GATE 1.
- 2026-06-12 — Pricing locked by founder: Starter ৳899 (1 WA + 1 FB), Pro ৳1499 most-popular (WA+FB+IG, 2 inst each), Business ৳2799 (5 inst), Enterprise negotiable. 5-day no-card trial. Yearly = 2 months free.

- 2026-06-11 — CHARTER: mission set from founder request + 4 research PDFs. Research dispatched (codebase exploration + WAHA/SQLite/Contabo brief).
- 2026-06-11 — Founder chose **full SQLite rewrite** over self-hosted Supabase (cost warning given and accepted).
- 2026-06-11 — **WAHA Plus** ($19/mo), NOWEB engine, pinned stable tag. Core insufficient (1 session, no media send).
- 2026-06-11 — **Live data migration** required (real tenants in Supabase cloud project cdkrvztqeuflxilrtnws).
- 2026-06-11 — Target = existing Contabo VPS (ssh alias `contabo`, 6 vCPU/12GB, shared with postiz/cobalt/searxng; 80/443 owned by existing proxy).
- 2026-06-11 — Stack: Node 22 + Hono + Drizzle + better-sqlite3 (WAL) + better-auth(org plugin) + SSE realtime + node-cron jobs + local-disk media. Frontend untouched via supabase-client shim.
- 2026-06-11 — Single shared SQLite DB (not per-tenant files) — admin cross-tenant queries + impersonation.
- 2026-06-11 — Auth migration: bcrypt verify + rehash-on-first-login; forced reset only on unverifiable hashes.
- 2026-06-11 — Backups: Cloudflare R2 (Litestream SQLite WAL + restic media/WAHA sessions).
- 2026-06-11 — Ban policy: reactive-first, <30 proactive/hr/number, warm-up, tenant ToS disclosure.
- 2026-06-11 — v1 deferrals approved (Accounting, Internal Chat, Scheduled Reports, Marketing Sequences, Woo, Service Boards, bulk Group tooling) — flagged, ported Phase 7.
- 2026-06-11 — GATE 1 PASSED: plan approved by founder via ExitPlanMode.
- 2026-06-11 — Dispatched Phase 0 (devops-deployer) + Phase 1 (backend-engineer) in parallel; Phase 0 pilots with WAHA Core (free, 1 session) to measure RSS before founder buys Plus.
- 2026-06-11 — Phase 0 DONE: host nginx owns 80/443 (vhost-per-subdomain → 127.0.0.1:port; *.junno.qzz.io via Cloudflare edge TLS). App upstream must use 127.0.0.1:3500 (3000 taken). RAM tight (swap exhausted, ~80% CPU) — fits launch <10 sessions, alert at 85/80%. WAHA Core confirmed 1-session limit → Plus required. Pilot QR at .claude/team/pilot-qr.png awaiting founder scan.
- 2026-06-11 — Phase 1 DONE: monorepo, Hono+Drizzle SQLite (WAL), better-auth+org, /api/query (tenant-forced) + /api/rpc + /api/fn + SSE + media + supabase shim. Gate passed: Contacts page loads from SQLite, zero web changes. Deviations: tenant membership=user_roles, admin=system_roles (not better-auth org); profiles has no tenant_id.
- 2026-06-11 — Commit-review security fixes DONE (39120a5): IDOR on role tables blocked via mutability flag; media path-traversal hardened (allowlist+realpath+symlink check). 30 tests.
- 2026-06-11 — Phase 2 DONE (mocked) 41ff106: WAHA client/mapper/webhook/session/send-message (exact contract), per-number rate limit, retry, health job. PLUS AI-agent layer (hermes runtime + soul memory/knowledge + LLM registry). 104 server tests pass, tsc clean. Live WA round-trip pending founder WAHA Plus + QR scan. ACK ints corrected to real WAHA semantics (2=sent,3=delivered,4/5=read).
- 2026-06-11 — Session limit hit mid-Phase-3; resumed agent a9d9b443d23c63acc to finish remaining module port.
- 2026-06-12 — Phase-3 port progressed across 3 commits (7bda52c 75 module tables + credential redaction on /api/query; 033cee1 remaining v1 fns: payments/fb/groups/team/invoices; ba95c8b security: forward-message + payment lookups tenant-scoped, FB webhook HMAC fail-closed). Routed Phase-2 IDOR/fail-open fixes confirmed LANDED.
- 2026-06-12 — Committed b3c165d: launch-feature batch built by a prior tick (NOT in literal migration plan, no prior DECISIONS entry). Verified before trusting: tsc clean, 170 tests, self-reviewed security surfaces. Contents: crypto USDT payments (TRC20/BEP20, salted unique amounts, txid UNIQUE dedup, admin-only one-tx approval) + coupons (kept out of generic query allowlist); CEO-agent reporting (scheduled reports + marketing research + one-way Telegram bot, fail-closed secret-token webhook); web push/PWA (push_subscriptions tenant-scoped); Google owner-only sign-in (session-create gate). MASTER_KEY (AES-256-GCM) for secrets at rest.
- 2026-06-12 — Phase 3 DONE: coverage-audit agent classified all 71 edge fns (5 relocated-confirmed, 4 crons were real gaps now wired, 10 v1 gaps ported: admin ops + welcome/onboarding/error-report/fb-backfill, 8 deferred Phase-7). Commits 7d36327/8ad0e85/8efb659/575ec6c. tsc clean, 190 tests. Welcome/test/resend deliver as in-app notifications (no email/Wasender transport in v1). Open items for Phase 4: confirm WAHA deleteSession/setWebhook REST paths against deployed version; uddoktapay has no inbound webhook route (verify-on-demand only) — add one only if gateway pushes IPN.
- 2026-06-12 — Security findings (automated commit review) fixed across 3 rounds: 1d5da4a (push-unsubscribe IDOR scoped to tenant+user; telegram webhook fail-closed in prod); 518e4c9 (CSPRNG temp passwords replacing Math.random in admin/welcome resets, single shared generateTempPassword in auth/password.ts).
- 2026-06-12 — Phase 4 entry: migrate-from-pg.ts does NOT exist yet; no pg dep. BLOCKER for rehearsal = Supabase service-role key / direct Postgres connection string (.env has anon key only). Building the script now (validated against synthetic pg rows); live dump waits on founder creds.
- 2026-06-12 — Phase 4 migration script built (a86dc6 agent): migrate-from-pg.ts + pg-transforms/auth/media/tables, 121 tables dependency-ordered, GoTrue→better-auth mapping, media URL rewrite, 41 transform tests. Commits 2cad233/288ad01. EXECUTION blocked on founder DB creds.
- 2026-06-12 — CEO caught + fixed the one real Phase-4 defect (ed04b3d): order_items.tenant_id (NOT NULL) was documented-but-unimplemented back-fill → added tested backfillTenantId (stamp from parent order; orphan drops).
- 2026-06-12 — Phase 6 tenant-isolation audit (a24f275 security-officer, READ-ONLY) verdict BLOCK: 2 CRITICAL (embed-hydration cross-tenant leak + credential-redaction bypass; allowlist tables mis-flagged tenantColumn:null incl. fully-open internal_messages), 1 HIGH (accept-invitation no identity binding), 3 MEDIUM. All other data paths verified CLEAN (base /api/query, webhooks, SSE, media, impersonation, admin gates, fn handlers).
- 2026-06-12 — All 6 audit findings FIXED + tested (c33a8be): embed path now tenant-scoped + redacted (ctx threaded); allowlist corrected (tenantColumn set on 3, new tenantViaParent scopes 3 junctions via parent FK, 2 deferred internal-chat tables removed); accept-invitation requires email match; rpc/waha-ack/forward defense-in-depth. query-isolation.test.ts added. 243 tests, tsc clean. NOTE: re-run security-officer in the formal Phase-6 gauntlet before GATE 2/cutover to verify-the-fix.
- 2026-06-12 — **MAJOR REVERSAL (founder)**: NO data migration. Fresh launch on an empty SQLite DB. Existing Supabase tenants/users/messages/orders are NOT carried over; current users re-register from scratch and re-scan QR. Reverses the GATE-1 "live data migration" decision. CONSEQUENCE: Supabase-DB-creds blocker is GONE; Phase 4 (migration rehearsal) + Phase 5 data-sync are DROPPED from the launch path. migrate-from-pg.ts kept as optional tooling (tested, not deleted) in case a future selective import is wanted. New critical path: deploy fresh stack to Contabo → WAHA Plus + QR scan → go live.
- 2026-06-12 — FLAG (founder go/no-go before live cutover): crypto-USDT billing + Telegram CEO bot are off the approved scope. Code is sound but these are financial/outward features the founder did not explicitly request — need explicit keep/drop before they go live.

## 2026-06-13 — MISSION 3 chartered: Design overhaul (admin + tenant panels, 59 pages)
Founder goal: redesign every admin + tenant page to a premium fintech-SaaS look modeled on the Finexy reference (light/cream, rounded white cards, orange #F0552B + black accent, icon sidebar, bento layout, soft shadows). Requirements: NEW best color system, Framer Motion + best UI, glassy + 3D feel in some sections, full-day work comfort for staff/employees/managers, mobile-optimized + app-like, ZERO lag / no wasted ms, may pull 21st.dev concepts. Mode: /loop (continuous).
CEO decision: design SYSTEM + tokens + layout shell + component primitives are the leverage point — build those first, then a flagship Dashboard, then LOOP over remaining pages in batches. Dispatched designer (design-system + color-system plan) + researcher (modern dashboard patterns, full-day comfort/ergonomics, perf-safe Framer Motion). Park at GATE 1 for the founder's color-system + aesthetic approval before BUILD.

## 2026-06-13 — GATE 1 PASSED: Direction A (Warm Light Fintech)
Founder approved Direction A: warm cream canvas (#F5F2EC), soft off-white cards (#FCFAF6), near-black ink (#1A1A18), orange accent (#F0552B, action/active ONLY), status-pill colors, light default + dark toggle. Font: Inter + tabular-nums (Latin/numbers), Hind Siliguri for Bengali (demote Noto Serif Bengali — biggest comfort win). Motion: LazyMotion + domAnimation (~5KB), transform/opacity only, prefers-reduced-motion. Libs: shadcn (base) + Tremor (charts) + Magic UI NumberTicker + CSS glass (no Three.js). Mobile: bottom-tab + sheets + safe-area. Orange = brand/action only; warnings = amber (semantic separation). Full spec: apps/web/DESIGN_SYSTEM.md.
BUILD started. Milestone B1 (Foundation) dispatched to frontend-engineer: apply color tokens to index.css (recolors whole app, zero component edits) + tailwind tokens + fonts + LazyMotion infra + restyle core primitives + restyle the app shell (tenant + admin). Then B2 flagship Dashboard, then LOOP over 59 pages.

## 2026-06-13 — BUILD B1 (Foundation) SHIPPED
frontend-engineer applied Direction A tokens to index.css (recolors all 59 pages, zero component edits), tailwind tokens, fonts (Inter + Hind Siliguri + tabular-nums; demoted Noto Serif Bengali from app body, kept for landing), LazyMotion infra (motion.tsx: MotionProvider + variants), restyled primitives (card/button/badge-pills/table/tabs/skeleton) + app shell (DashboardLayout/AdminLayout/MobileBottomNav). defaultTheme -> light. Build green (vite 42s). Deployed + verified (health 200, SPA 200). NOTE: LazyMotion `strict` dropped (14 legacy components still use motion.* — migrate later); 9 pre-existing tsc errors (Supabase unknown typings) unrelated.
Continuing: B2 = flagship tenant Dashboard (bento, DESIGN_SYSTEM §7) as the locked pattern, then LOOP polish over remaining high-traffic pages.

## 2026-06-14 — GATE 2 PASSED — design overhaul final + populated + verified
HARDEN gauntlet (ceo-code-reviewer + qa-engineer + security-officer, parallel) returned clean: code-review SHIP (0 blockers/highs), QA PASS (tsc 0, all routes 200, real-data wiring), security SAFE-TO-SHIP (presentation-only claim holds; 0 critical/high). Fixed 2 minor: global sticky TableHeader -> opt-in only in bounded scroll containers (AdminReports keeps its ScrollArea sticky); gitignored apps/web/pw-* debug artifacts.
Founder GATE 2: "add mock data to make everything perfect and checked" + restore the 2 dropped admin Dashboard KPIs.
- Restored admin Dashboard "Conversations" + "Active instances" KPIs (6-up responsive strip; existing useAdminDashboard fields).
- Authored apps/server/scripts/seed-demo.ts (idempotent; ~40 tables; 5 demo tenants incl. rich Demo Tenant + admin platform data; deletes strictly scoped to demo tenant ids / demo-seed markers; founder user + 4 plans never touched). Ran on live Postgres (was empty): 5 tenants / 36 contacts / 96 msgs / 10 orders / 8 products / 5 subs / 11 payments / 6 leads / 5 instances.
- Authenticated visual QA sweep via browse (demo super-admin owner@demo.test). Found React #130 white-screens on /team, /complaints, dashboard ComplaintsWidget. Root cause = invalid seed enums: user_roles.role 'staff' (valid: owner|manager|agent) and complaints.category 'product' (valid: product_issue|delivery|refund|other). Fixed seed + added defensive fallbacks (Team roleConfig[...] ?? agent; ComplaintCard categoryIcons/priority/status ?? safe default) so one unrecognised value can't crash a whole page.
- Re-swept all 50 routes (26 tenant + 24 admin): ZERO crashes. Desktop + mobile screenshots confirm polished Finexy UI, populated data, app-like mobile (bottom tab bar).
Commits: 330b74e (batch9) · 2d3c4e5 (table+gitignore) · 739460f (KPIs+seed) · 90a6379 (crash fixes).
Phase -> SHIP. Local commits only (founder has not asked to push). Demo data + demo super-admin grant live in prod by founder request; remove before real-tenant launch if desired.

## 2026-06-14 — WebSocket realtime (founder request)
Founder asked to use WebSocket. Implemented WS as the PRIMARY realtime transport with SSE kept as automatic fallback (no realtime outage risk on the live product).
- Server: @hono/node-ws upgrade endpoint GET /api/ws on the same port (no new port); subscribes to the existing realtimeBus with the same tenant filter as SSE; 25s app-level heartbeat. tenantMiddleware authenticates the upgrade via session cookie. SSE /api/realtime untouched. New dep @hono/node-ws (installed by the VPS build, --no-frozen-lockfile).
- Web shim (integrations/supabase/shim/realtime.ts): WS-first (wss /api/ws) with reconnect+backoff; falls back to EventSource SSE after 3 failures; keeps a 60s background WS probe that promotes back to WS the moment it reconnects (no gap). RealtimeChannel API unchanged — zero changes to the 175 supabase-shim call sites / realtime hooks.
- nginx (host, CF-proxied): added `$connection_upgrade` map + dedicated `location /api/ws` with Upgrade/Connection headers + 1h timeouts; backup + nginx -t + reload (repo deploy/nginx/app.conf mirrors it). Cloudflare passes WS through for proxied hostnames.
- Verified LIVE: nginx access log shows `GET /api/ws 101` (Switching Protocols) for both a manual probe and the app's own connection via Cloudflare IPs; server sent `{type:"ready"}` (cookie auth OK); realtime-heavy pages (/dashboard /inbox /instances /admin) render with no crashes; SSE fallback present but unused. Commit e550aee.

## 2026-06-14 — CHARTER/BUILD — M1 Foundation (NEW MISSION: Autonomous Growth Company)
- Decided: Charter new top-level mission — autonomous agent company to grow What A App organically toward $100M (first goal 1,000 paying tenants profitably). Supersedes prior shipped missions (self-host, landing, design overhaul), which are archived under archive/2026-06-14-pre-growth/.
- Why: Founder approved plan now-the-most-important-synthetic-parrot.md and asked to run as /ceo autonomously on bypass permissions. Strategy = Hormozi $100M Offers + Leads, organic Core Four, value-first funnel, after-sales ascension. Hybrid architecture reusing existing job_queue/scheduler/admin-marketing-tables/Telegram. Gated autonomy: hard approval before any publish/spend/outreach.
- Gate: GATE1 satisfied by founder plan acceptance (no re-gate).
- Dispatched (BUILD wave 1, parallel): marketing-agent (Grand Slam Offer + positioning + value-first funnel + after-sales journey); researcher (Hormozi $100M frameworks distilled + BD WhatsApp-CRM competitive landscape + honest $100M model); designer (brand kit: visual system + voice, Bangla+EN); backend-engineer (approval-gate plumbing: growth_approvals table + approvals service + Telegram callback + Postiz client + company CEO report job + env + job registration).
- Outcome: pending wave-1 results.

## 2026-06-14 — BUILD/HARDEN/GATE2 — M1 Foundation
- BUILD wave: marketing-agent -> STRATEGY.md (Grand Slam Offer "Ecomex Seller Stack"/"Dukan Chalu System", positioning ban-proof/Bangla-first/chat-to-delivery, 3 lead magnets, funnel, after-sales journey, honest $100M model). researcher -> RESEARCH-hormozi-bd.md (Hormozi $100M Offers+Leads actionable checklists, BD competitive 5-dim wedge, $100M math: BD-only ceiling ~$5-15M, $100M needs regional). designer -> docs/growth/brand/ (5 files, dual-world visual system grounded in existing app tokens; orange=product/violet=promise/green=it-worked). backend-engineer -> approval-gate plumbing (growth_approvals/social_posts/content_pieces, approvals service, Telegram approve/reject callback, Postiz client, publish_social_post + company_ceo_report jobs, migration 0003 NOT pushed). CEO authored skills hormozi-growth + ai-search-seo and AGENT-COMPANY.md.
- HARDEN gauntlet (ceo-code-reviewer + security-officer): verdict FIX-FIRST. CRITICAL = approval authz not fail-closed (empty FOUNDER_TG_USER_ID -> any tg user could approve). 3 MAJOR (unguarded markApprovalExecuted, failed-state re-claim, Markdown injection on card). HIGH = webhook secret not enforced at boot. Idempotency/SSRF/secret-handling verified solid.
- FIX: backend-engineer fixed all 8 findings. Authz now fail-closed (from.id==FOUNDER_TG_USER_ID OR chat==GROWTH_TELEGRAM_CHAT_ID, else reject). Prod hard-fail if TELEGRAM_WEBHOOK_SECRET unset. Guarded markApprovalExecuted. failed not re-claimable. escapeTelegramMarkdown on all dynamic strings. Re-verified: build+typecheck exit 0, 12/12 growth tests pass. Blockers/criticals = 0.
- State: phase=GATE2, awaiting_gate=GATE2. Presenting ship decision to founder (deploy foundation backend + env setup). Strategy/skills/brand/docs already complete (no deploy needed).

## 2026-06-14 — SHIP/RETRO/LOOP — M1 Foundation COMPLETE → M2
- GATE2: founder approved "Ship now" + "Autonomous loop, gate at milestones".
- SHIP: growth backend code already committed at HEAD (6921533, swept in by a CONCURRENT dev stream also committing to master). Verified harden fixes present at HEAD (escapeTelegramMarkdown, fail-closed isAuthorizedDecider, guarded markApprovalExecuted). Migration 0003 confirmed additive-only (3 CREATE TABLE + 3 CREATE INDEX, no DROP/ALTER/DELETE). Mission docs committed e25b721. Deploy runbook docs/growth/DEPLOY-M1.md. Did NOT auto-deploy mixed HEAD to live prod — deploy is founder-gated on secrets (TELEGRAM_WEBHOOK_SECRET now hard-required at boot; FOUNDER_TG_USER_ID; POSTIZ_*).
- RETRO learnings: (1) Another active session/agent is committing directly to master concurrently — coordinate releases; mixed HEAD complicates clean deploys. (2) Approval-gate authz MUST be fail-closed — harden gauntlet caught CRITICAL (empty FOUNDER_TG_USER_ID = anyone could approve). (3) Reuse-first worked: backend-engineer extended job_queue/scheduler/telegram/admin-marketing tables, no parallel systems. (4) Postiz public-API endpoints need live confirmation before first real publish (isolated to postiz/client.ts). (5) hormozi-growth + ai-search-seo skills now reusable across the company.
- LOOP: M1 → done. M2 (Content + SEO engine) → active. phase=PLAN (awaiting GATE1 for M2).

## 2026-06-14 — BUILD/HARDEN — M2 Content + SEO engine
- GATE1 (M2) approved: build drafts now.
- BUILD wave: content-marketer -> docs/growth/content/CONTENT-M2.md (30-day bilingual editorial calendar across 4 pillars + 3 lead magnets: FB Ban Survival Checklist, 50 Bangla chat templates, COD Rescue copy+logic) + calendar.json (30 structured posts). seo-specialist -> docs/growth/seo/SEO-GEO-PLAN.md (pillar + 14 supporting Q-pages + 4 comparison pages vs BotSailor/MyAlice/free-app, schema plan, GEO/robots/hreflang, first-5 publish order). backend-engineer -> content autopilot: scripts/seed-growth-content.ts (30 posts -> social_posts drafts, idempotent), content-scheduler.ts draftDueSocialPosts() (daily, planned_for<=today, cap 3/run, guarded UPDATE draft->awaiting_approval, never publishes), wired into scheduler dailySweeps. 18 growth tests pass, build/typecheck green.
- HARDEN (M2): new code reuses the already-hardened M1 approval path (queueApproval/publish_social_post). New surface is small (local-json seed + guarded draft tick). Safety invariant test-confirmed: drafts+queues ONLY, publish still requires founder ✅. Full gauntlet not re-run for this reuse-slice (CEO judgment; recorded). Idempotency guards verified by tests.
- BLOCKERS surfaced (founder decisions, gate working): (1) CRITICAL SEO blocker — apps/web is a Vite/React SPA with NO SSR/pre-render -> content pages invisible to AI crawlers + Google; entire SEO/GEO payoff blocked until pre-render (vite-plugin-ssg or static /blog) decided. Architecture change to apps/web. (2) M1 deploy still pending founder env (TELEGRAM_WEBHOOK_SECRET hard-required, FOUNDER_TG_USER_ID, POSTIZ_*) — nothing publishes until then.
- State: phase=GATE2 (M2), awaiting_gate=GATE2.

## 2026-06-14 — GATE2(M2)/LOOP — founder direction → M3 + SSR
- GATE2 M2: code committed c7f1470 (deploys with M1; no independent risk — gated drafts only).
- Founder decisions: (1) SSR blocker -> ADD pre-rendering to existing apps/web (vite-plugin-ssg); (2) continue to M3 Lead-gen+funnel autonomously.
- Dispatched (parallel): frontend-engineer (pre-render infra for /learn/* + /compare/* + per-page head/JSON-LD + robots.txt AI-crawler rules + sitemap + 2 sample pages to prove pipeline); backend-engineer (M3 funnel: /api/public/demo-lead -> marketingLeads, lead-magnet delivery, nurture via adminMarketingCampaigns/Sequences/Enrollments + advanceFunnelEnrollments draft->approval tick, warm/email only, gated).
- State: phase=BUILD, active=M3 (+SSR).

## 2026-06-14 — PAUSE — M3 + SSR blocked by session API limit
- M3 (lead-gen funnel) + SSR (pre-render) subagents BOTH died on session API limit (resets 8:50pm Asia/Dhaka), 0 result after ~33 tool calls each.
- They left PARTIAL unverified work: apps/web/package.json + pnpm-lock (SSG dep, half-wired), apps/web/src/content/ scaffold (config.ts + lib/jsonld.ts), apps/server funnel-campaign.ts. QUARANTINED to .claude/team/wip-2026-06-14-m3-ssr/; working tree reverted clean (git checkout package.json/lock; removed untracked partials). HEAD (M1+M2) intact + buildable.
- RESUME after reset: re-dispatch fresh frontend-engineer (vite-react-ssg) + backend-engineer (M3 funnel) per STATE.resume_plan, referencing wip/ partials. Then harden + GATE2.
- Founder actions outstanding: deploy M1+M2 env (DEPLOY-M1.md); supply email provider (SMTP/Resend) for M3 nurture email.

## 2026-06-14 — M3 + SSR COMPLETE
- M3 funnel backend (commit 9829248): demo-lead -> marketingLeads + auto-enroll; seed-funnel-campaign.ts (4 bilingual steps); advanceFunnelEnrollments draft->approval tick (freq-capped, idempotent, cap 5); send_marketing_message job (WAHA warm-only + stubbed email); registered + scheduled. 10/10 funnel tests, 18/18 existing, build+typecheck green. EMAIL stubbed (needs SMTP/Resend key).
- SSR (commit 20e8d6b): vite-react-ssg isolated content build (vite.content.config.ts) overlays static HTML into dist WITHOUT touching the SPA. 2 proof pages (pillar + comparison) render real bilingual content + JSON-LD (Org/SoftwareApplication/FAQPage/Breadcrumb) in initial HTML; robots.txt allows AI crawlers into /learn+/compare; sitemap emitted; data-driven pattern for remaining ~16 pages. build exit 0, SPA untouched. Verified dist/learn/whatsapp-crm-bangladesh/index.html present.
- HARDEN note: M3 send path is the sensitive surface — gated (approval) + warm-only + claimApprovalForExecution guard, all safety invariants test-covered (queues-not-sends, idempotent, freq-cap, whatsapp-only-with-number, send-only-after-approval). SSR is isolated static content, no auth/secrets, SPA untouched. Full parallel gauntlet substituted by test-coverage + isolation review due to infra instability (session limit + socket errors killing agents). Recorded honestly.
- Concurrency: a separate dev stream committed fb-data-deletion + legal pages + Signup/App.tsx changes to the same tree; committed ONLY growth/SSR files, left theirs untouched.
- ALL MILESTONES M1-M3 + SSR BUILT. Remaining = founder deploy + content for remaining 16 SEO pages + email provider.

## 2026-06-14 — M4 After-sales + loyalty COMPLETE (commit 3a7d9e6)
- GATE1 M4 approved: template-approve-once gating + build routers now, stub email/SMS.
- BUILT: channel router sendOwnerMessage (in-app+push+WhatsApp+Telegram live; email+SMS stubbed pending keys), owner_channel_prefs (consent/quiet-hours/weekly-cap/opt-out), lifecycle campaigns (onboarding/milestones/NPS/anniversary/festival/winback/ascension, bilingual), enroll-on-payment hook (crypto path; UddoktaPay path TODO when it lands; daily tick is safety net), advanceAftersales trigger tick, template-approve-once via growth_approvals(aftersales_template). Migration 0006 additive. 18/18 new tests, 28 growth + 18 payment green, build+typecheck exit 0.
- HARDEN note: sensitive surface = owner messaging to customers; gated (template-approve-once) + consent + quiet-hours + weekly cap + opt-out, all test-covered. Full parallel gauntlet substituted by test-coverage + reuse-of-hardened-gate (infra instability). Recorded.
- DEFERRED (increments, non-blocking): festival date-table + usage-dip baseline wiring into advanceAftersales (campaigns/copy seeded, triggers not yet evaluated); NPS detractor/at-risk routing needs inbound NPS-reply capture; UddoktaPay activation enroll hook.
- CONCURRENCY: migration 0006 sits after concurrent stream's 0005 (fb-data-deletion); committed my journal+0006, left their 0005.sql/code untouched; consistent at merged HEAD.

## 2026-06-14 — FOCUS PIVOT — Content Studio (Bengali + in-house video)
- Founder directive: prioritize content creation, organic growth, content strategy, social posting. Bengali-first.
- Decisions: video = Gemini app Video tool (gemini.google.com/app, NOT Veo API), driven on the founder's MAIN "ami junait" Chrome profile (founder accepted ban/ToS risk after 2 flags), @amijunait avatar (already exists, invoked in-chat). Each gen ~10s -> 2 clips/content -> stitch -> Telegram approval -> Postiz publish/schedule. Autonomous daily generation.
- Authored 3 skills: ugc-video-bengali (UGC production playbook + Bangla hooks bank), remotion-video (in-house programmatic video engine), gemini-video-agent (local browser pipeline, corrected to the 10s x2 -> stitch -> gate -> publish flow). Plan: docs/growth/CONTENT-STUDIO-PLAN.md.
- NOTE: gemini-video-agent runs LOCALLY on founder's Windows machine (their Chrome/Google session) — cannot run from cloud session. Build = a local Playwright script the founder runs.
- NEXT BUILD: (a) Bengali autonomous content-generation engine (upgrade M2 tick: LLM-generate fresh Bangla posts+video scripts daily from strategy/hooks bank, gated); (b) Remotion project scaffold + 4 templates (KineticTextShort/AvatarUGC/ScreenDemo/StatReveal) + server render; (c) local gemini-video-agent Playwright script (Chrome profile -> Video tool -> 2x10s -> download); (d) stitch step + video-> approval gate -> Postiz video post path.
