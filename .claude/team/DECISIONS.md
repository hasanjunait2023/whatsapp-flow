# DECISIONS

- 2026-06-12 — MISSION 2 CHARTER: high-conversion sales landing page. RESEARCH (BD f-commerce conversion/pricing/pixels) + DESIGN (LANDING_DESIGN.md from dark AI reference) dispatched in parallel; both delivered. Research confirms market white-space: no Bangla-first, COD/courier-aware, bKash-native WhatsApp CRM competitor; 3-tier decoy validated; pixel taxonomy locked.
- 2026-06-12 — FOUNDER added FLAGSHIP value prop: **customer-data ownership + ban-proofing**. On Meta-owned channels the seller doesn't own their data; a page/number ban = lost customers + lost data. We store their contacts/chats/orders securely (survives a ban), make it exportable/reusable, and offer follow-up + funnel automation to re-engage — plus anti-ban WhatsApp safety. This becomes a hero-adjacent flagship section + a real data-export feature; funnel automation = fast-follow (Marketing module). HONEST scope flagged at GATE 1.
- 2026-06-12 — Pricing locked by founder: Starter ৳899 (1 WA + 1 FB), Pro ৳1499 most-popular (WA+FB+IG, 2 inst each), Business ৳2799 (5 inst), Enterprise negotiable. 5-day no-card trial. Yearly = 2 months free.

- 2026-06-11 — CHARTER: mission set from founder request + 4 research PDFs. Research dispatched (codebase exploration + WAHA/SQLite/Contabo brief).
- 2026-06-11 — Founder chose **full SQLite rewrite** over self-hosted Supabase (cost warning given and accepted).
- 2026-06-11 — **WAHA Plus** ($19/mo), NOWEB engine, pinned stable tag. Core insufficient (1 session, no media send).
- 2026-06-11 — **Live data migration** required (real tenants in Supabase cloud project cdkrvztqeuflxilrtnws).
- 2026-06-11 — Target = existing Contabo VPS (ssh alias `contabo`, 6 vCPU/12GB, shared with postiz/cobalt/searxng; 80/443 owned by existing proxy).
- 2026-06-11 — Stack: Node 22 + Hono + Drizzle + better-sqlite3 (WAL) + better-auth(org plugin) + SSE realtime + node-cron jobs + local-disk media. Frontend untouched via supabase-client shim.
- 2026-06-11 — Single shared SQLite DB (not per-tenant files) — admin cross-tenant queries + impersonation.
- 2026-06-11 — Auth migration: bcrypt verify + rehash-on-first-login; forced reset only on unverifiable hashes.
- 2026-06-11 — Backups: Cloudflare R2 (Litestream SQLite WAL + restic media/WAHA sessions).
- 2026-06-11 — Ban policy: reactive-first, <30 proactive/hr/number, warm-up, tenant ToS disclosure.
- 2026-06-11 — v1 deferrals approved (Accounting, Internal Chat, Scheduled Reports, Marketing Sequences, Woo, Service Boards, bulk Group tooling) — flagged, ported Phase 7.
- 2026-06-11 — GATE 1 PASSED: plan approved by founder via ExitPlanMode.
- 2026-06-11 — Dispatched Phase 0 (devops-deployer) + Phase 1 (backend-engineer) in parallel; Phase 0 pilots with WAHA Core (free, 1 session) to measure RSS before founder buys Plus.
- 2026-06-11 — Phase 0 DONE: host nginx owns 80/443 (vhost-per-subdomain → 127.0.0.1:port; *.junno.qzz.io via Cloudflare edge TLS). App upstream must use 127.0.0.1:3500 (3000 taken). RAM tight (swap exhausted, ~80% CPU) — fits launch <10 sessions, alert at 85/80%. WAHA Core confirmed 1-session limit → Plus required. Pilot QR at .claude/team/pilot-qr.png awaiting founder scan.
- 2026-06-11 — Phase 1 DONE: monorepo, Hono+Drizzle SQLite (WAL), better-auth+org, /api/query (tenant-forced) + /api/rpc + /api/fn + SSE + media + supabase shim. Gate passed: Contacts page loads from SQLite, zero web changes. Deviations: tenant membership=user_roles, admin=system_roles (not better-auth org); profiles has no tenant_id.
- 2026-06-11 — Commit-review security fixes DONE (39120a5): IDOR on role tables blocked via mutability flag; media path-traversal hardened (allowlist+realpath+symlink check). 30 tests.
- 2026-06-11 — Phase 2 DONE (mocked) 41ff106: WAHA client/mapper/webhook/session/send-message (exact contract), per-number rate limit, retry, health job. PLUS AI-agent layer (hermes runtime + soul memory/knowledge + LLM registry). 104 server tests pass, tsc clean. Live WA round-trip pending founder WAHA Plus + QR scan. ACK ints corrected to real WAHA semantics (2=sent,3=delivered,4/5=read).
- 2026-06-11 — Session limit hit mid-Phase-3; resumed agent a9d9b443d23c63acc to finish remaining module port.
- 2026-06-12 — Phase-3 port progressed across 3 commits (7bda52c 75 module tables + credential redaction on /api/query; 033cee1 remaining v1 fns: payments/fb/groups/team/invoices; ba95c8b security: forward-message + payment lookups tenant-scoped, FB webhook HMAC fail-closed). Routed Phase-2 IDOR/fail-open fixes confirmed LANDED.
- 2026-06-12 — Committed b3c165d: launch-feature batch built by a prior tick (NOT in literal migration plan, no prior DECISIONS entry). Verified before trusting: tsc clean, 170 tests, self-reviewed security surfaces. Contents: crypto USDT payments (TRC20/BEP20, salted unique amounts, txid UNIQUE dedup, admin-only one-tx approval) + coupons (kept out of generic query allowlist); CEO-agent reporting (scheduled reports + marketing research + one-way Telegram bot, fail-closed secret-token webhook); web push/PWA (push_subscriptions tenant-scoped); Google owner-only sign-in (session-create gate). MASTER_KEY (AES-256-GCM) for secrets at rest.
- 2026-06-12 — Phase 3 DONE: coverage-audit agent classified all 71 edge fns (5 relocated-confirmed, 4 crons were real gaps now wired, 10 v1 gaps ported: admin ops + welcome/onboarding/error-report/fb-backfill, 8 deferred Phase-7). Commits 7d36327/8ad0e85/8efb659/575ec6c. tsc clean, 190 tests. Welcome/test/resend deliver as in-app notifications (no email/Wasender transport in v1). Open items for Phase 4: confirm WAHA deleteSession/setWebhook REST paths against deployed version; uddoktapay has no inbound webhook route (verify-on-demand only) — add one only if gateway pushes IPN.
- 2026-06-12 — Security findings (automated commit review) fixed across 3 rounds: 1d5da4a (push-unsubscribe IDOR scoped to tenant+user; telegram webhook fail-closed in prod); 518e4c9 (CSPRNG temp passwords replacing Math.random in admin/welcome resets, single shared generateTempPassword in auth/password.ts).
- 2026-06-12 — Phase 4 entry: migrate-from-pg.ts does NOT exist yet; no pg dep. BLOCKER for rehearsal = Supabase service-role key / direct Postgres connection string (.env has anon key only). Building the script now (validated against synthetic pg rows); live dump waits on founder creds.
- 2026-06-12 — Phase 4 migration script built (a86dc6 agent): migrate-from-pg.ts + pg-transforms/auth/media/tables, 121 tables dependency-ordered, GoTrue→better-auth mapping, media URL rewrite, 41 transform tests. Commits 2cad233/288ad01. EXECUTION blocked on founder DB creds.
- 2026-06-12 — CEO caught + fixed the one real Phase-4 defect (ed04b3d): order_items.tenant_id (NOT NULL) was documented-but-unimplemented back-fill → added tested backfillTenantId (stamp from parent order; orphan drops).
- 2026-06-12 — Phase 6 tenant-isolation audit (a24f275 security-officer, READ-ONLY) verdict BLOCK: 2 CRITICAL (embed-hydration cross-tenant leak + credential-redaction bypass; allowlist tables mis-flagged tenantColumn:null incl. fully-open internal_messages), 1 HIGH (accept-invitation no identity binding), 3 MEDIUM. All other data paths verified CLEAN (base /api/query, webhooks, SSE, media, impersonation, admin gates, fn handlers).
- 2026-06-12 — All 6 audit findings FIXED + tested (c33a8be): embed path now tenant-scoped + redacted (ctx threaded); allowlist corrected (tenantColumn set on 3, new tenantViaParent scopes 3 junctions via parent FK, 2 deferred internal-chat tables removed); accept-invitation requires email match; rpc/waha-ack/forward defense-in-depth. query-isolation.test.ts added. 243 tests, tsc clean. NOTE: re-run security-officer in the formal Phase-6 gauntlet before GATE 2/cutover to verify-the-fix.
- 2026-06-12 — **MAJOR REVERSAL (founder)**: NO data migration. Fresh launch on an empty SQLite DB. Existing Supabase tenants/users/messages/orders are NOT carried over; current users re-register from scratch and re-scan QR. Reverses the GATE-1 "live data migration" decision. CONSEQUENCE: Supabase-DB-creds blocker is GONE; Phase 4 (migration rehearsal) + Phase 5 data-sync are DROPPED from the launch path. migrate-from-pg.ts kept as optional tooling (tested, not deleted) in case a future selective import is wanted. New critical path: deploy fresh stack to Contabo → WAHA Plus + QR scan → go live.
- 2026-06-12 — FLAG (founder go/no-go before live cutover): crypto-USDT billing + Telegram CEO bot are off the approved scope. Code is sound but these are financial/outward features the founder did not explicitly request — need explicit keep/drop before they go live.
