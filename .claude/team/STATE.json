{
  "mission": "Self-host whatsapp-flow on Contabo VPS: WAHA Plus + SQLite migration + launch",
  "phase": "BUILD",
  "active_milestone": "DEPLOYED app-only to Contabo at /srv/whatsapp-flow. Image builds, container Up(healthy) on 127.0.0.1:3500 (fresh SQLite, migrations ran), nginx vhost whataapp.junno.qzz.io installed + reloaded, origin verified (nginx->app->SPA 200, healthz green). WAHA CORE now running (founder: Plus later when onboarding customers) — devlikeapro/waha:latest, NOWEB, CORE tier, single-session, API-key auth validated, app<->waha connected internally. ADMIN ACCOUNT CREATED on live DB (founder request): email amijunait@gmail.com registered via better-auth (password bcrypt/scrypt-hashed, stored ONLY in DB user record — never committed), granted system_roles role=admin + is_super_admin=1; sign-in verified HTTP 200. REMAINING to go live: (1) founder adds Cloudflare DNS A record whataapp.junno.qzz.io -> 13.140.152.96 orange-cloud + SSL Full; (2) after DNS: founder logs in as admin, creates instance, scans QR (single session, Core) for first WhatsApp round-trip; (3) litestream/restic not started (no R2 creds yet). Pilot config tracked in docker-compose.override.yml.",
  "awaiting_gate": null,
  "gates_passed": ["GATE1 2026-06-11 plan approved by founder"],
  "founder_actions_pending": ["Buy WAHA Plus ($19/mo) for multi-session+media", "Create Cloudflare R2 bucket+token (off-VPS backups)", "Scan instance QR with WhatsApp number once deployed (first live round-trip test)", "Pick public hostname for the app (e.g. app.junno.qzz.io) + point Cloudflare DNS at VPS", "GO/NO-GO on off-scope features: crypto-USDT billing + Telegram CEO bot (built+tested; keep or drop)", "Provide MASTER_KEY + any LLM provider key for production .env (names documented in .env.example)"],
  "phases": {
    "0_infra": "done",
    "1_backend_skeleton": "done",
    "security_fixes": "done (commit 39120a5)",
    "2_waha_slice": "done (mocked; live pending founder QR+Plus)",
    "3_module_port": "done (71 edge fns classified; v1 gaps ported in 7d36327/8ad0e85/8efb659/575ec6c; 3 security rounds fixed: 1d5da4a push/telegram, 518e4c9 CSPRNG temp passwords)",
    "4_migration_rehearsal": "DROPPED from launch path (founder chose fresh launch, no data migration). Script kept as optional tooling.",
    "deployment_build": "done (a03c696..0752cc2): Dockerfile (multi-stage, fresh-DB migrations on boot, non-root, /healthz), docker-compose (app 127.0.0.1:3500/1500m + waha NOWEB internal/2500m + litestream + restic), nginx SSE-safe vhost, litestream->R2, .env.production.example, deploy.sh, runbook. CAVEATS: docker not available locally so image build UNVERIFIED (must `docker compose build` on VPS first); WAHA Plus tag is a guess (private paid image — founder must docker-login + confirm exact tag from their Plus dashboard).",
    "6_hardening": "write-load/contention test done (73ed627) + synchronous=NORMAL perf win. 248 tests. Remaining: ban-risk UI guardrails (needs founder product input on caps/copy), formal re-audit gauntlet before cutover.",
    "5_cutover": "pending",
    "6_hardening_launch": "pending",
    "7_deferred_modules": "pending"
  },
  "post_launch_session": "2026-06-12 (cont.): Founder directed completing the remaining backlog. DONE + DEPLOYED LIVE: (1) crypto-USDT + Telegram CEO bot KEPT — verified fully wired (BILLING/CEO/PUSH handlers + telegram webhook + CEO jobs) + 34 tests green; dormant until env vars set. (2) Ban-risk guardrails: server cap already 29/hr proactive (reactive bypass) + NEW BanRiskNotice (warm-up + unofficial-API ToS ack) gating both connect paths, persisted per-tenant. (3) R2 backups: litestream+restic config complete; FIXED restic env_file/${} precedence footgun; added deploy/restore-drill.sh; awaits founder R2 creds. (4) Fixed dead-Supabase webhook URLs (WhatsApp instance, FB, Woo) -> window.location.origin + real backend paths. (5) SECURITY RE-AUDIT (security-reviewer agent) returned NO-GO with 3 HIGH blockers + 3 hardening — ALL FIXED + DEPLOYED: F1 message_templates cross-tenant read/write (added tenant_id col migration 0010 + scoping + regression tests), F3 no server-side instance cap (enforce plan max_instances in create-session, admins bypass), F7 MASTER_KEY lazy->eager startup validation (prod), F6 uddoktapay-verify gateway-call-before-tenant-check reorder, F4 unbounded IN() arrays capped at 500, F8 Math.random->randomInt CSPRNG. 251 tests green, tsc clean, deployed to VPS (healthz 200, migration 0010 applied, admin intact). NOW GO for customers on security. REMAINING Phase 7: Internal Chat (needs tenant_id migration+scope), Bulk Group tooling (buildable), Courier BD (needs founder provider+API key). Marketing Sequences DEFERRED by founder. Outstanding dead-Supabase refs in admin tools (WebhookSecretManager, ResetPasswordDialog) + broken fetches (useExternalSales, presence hooks) flagged for follow-up sweep.",
  "phase7_session": "2026-06-12 (cont.): DNS live (whataapp.junno.qzz.io -> Cloudflare 200, healthz 200). Built+tested+DEPLOYED 3 Phase-7 modules: (a) COURIER BD — BYOK per-tenant (Steadfast booking/tracking + BDCourier risk check; creds encrypted, courier_integrations read-only+redacted, save via courier-save-integration fn; Pathao booking deferred-OAuth); (b) INTERNAL CHAT — dedicated membership-scoped route internal-chat-fns.ts (list/create-direct/create-group/add-remove-member/send/mark-read), useInternalChat rewritten to fns, cross+intra-tenant DM authz tested; (c) BULK GROUP TOOLING — group-queue-batch enqueue + group-batch-processor + scheduler worker, paced batches + per-tenant daily cap (tenant_daily_group_limits) enforced. Also fixed dead-Supabase webhook URLs (WhatsApp/FB/Woo). 282 tests green. CONCURRENT WIP from another editor present + UNTOUCHED: FB OAuth (fb-oauth.ts, services/facebook/, index.ts/env.ts) — uncommitted, has 3 tsc errors, NOT in deployed HEAD. Marketing Sequences DEFERRED by founder. Remaining Phase 7: Accounting, Service Boards, WooCommerce sync, Pathao OAuth, Scheduled-report email cron.",
  "updated": "2026-06-12"
}
